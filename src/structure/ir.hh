#pragma once

#include <vector>
#include <string>
#include <map>

#include "../common.hh"
#include "SymTable.hh"

using std::vector;
/*
class BasicBlock;
class Insturction;

class BasicBlock : public common {
    VariableTable cur_var_table;
    vector<common *> block_commponent;
};

class ThreeAddrCode : public common {
    Operator op;
};*/
